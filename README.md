# Leap Watcher
A blockchain validation tool for LeapDAO Plasma Chain

# Installation
`git clone repoUrl`

`npm install`

Edit `server/config`

# Running app
`npm run dev`

app runs @ http://serverip:3000
